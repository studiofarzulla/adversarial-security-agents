# MCP RAG Server

**Note**: Full MCP server code will be added in next commit.

The MCP server provides semantic search over 5,395 offensive security documents via Model Context Protocol.

## Components (to be added)

- `server.py` - Flask MCP server
- `Dockerfile` - Container build
- `deployment.yaml` - K8s manifest
- `requirements.txt` - Python dependencies

See `../docs/MCP-PROTOCOL.md` for implementation details.
