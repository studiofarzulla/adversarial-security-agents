# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
*.egg-info/
dist/
build/

# Logs (may contain sensitive command output)
*.log
logs/
/var/log/agent/

# EXCEPT: Allow sanitized example logs
!examples/logs/
!examples/logs/*.log
!examples/logs/*.md

# Environment variables (may contain IP addresses, credentials)
.env
.env.local
*.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Kubernetes secrets (NEVER commit these)
*-secret.yaml
*secret*.yaml

# Personal configuration (IPs, passwords, etc.)
config.local.yaml
config.local.yml
.env.local
target-ips.txt
lab-config.yaml

# MCP server data (large files)
/mcp-server/data/
/mcp-server/indexes/
/mcp-server/*.faiss
/mcp-server/documents.json

# Container images (too large for git)
*.tar
*.tar.gz
*.tar.bz2

# Temporary files
/tmp/
*.tmp
*.temp

# Attack logs (may contain sensitive target info)
attack-logs/
command-history/
